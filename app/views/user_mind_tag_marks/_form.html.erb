<%= form_for(@user_mind_tag_mark) do |f| %>
  <% if @user_mind_tag_mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_mind_tag_mark.errors.count, "error") %> prohibited this user_mind_tag_mark from being saved:</h2>

      <ul>
      <% @user_mind_tag_mark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mark %><br>
    <%= f.number_field :mark %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
