<%= form_for(@mind_stream) do |f| %>
  <% if @mind_stream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mind_stream.errors.count, "error") %> prohibited this mind_stream from being saved:</h2>

      <ul>
      <% @mind_stream.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
